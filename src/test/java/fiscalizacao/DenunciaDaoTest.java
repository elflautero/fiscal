package fiscalizacao;

public class DenunciaDaoTest {
	
/*
	@Test
	public void test() {
	
	Subterranea sGeral = new  Subterranea();
	
	DatePicker dpDataSubterranea = new DatePicker();
	
	DateTimeFormatter formatter = new DateTimeFormatterBuilder()
			.parseCaseInsensitive()
			.append(DateTimeFormatter.ofPattern("dd/MM/yyyy"))
			.toFormatter();
	
	
		
	dpDataSubterranea.setConverter(new StringConverter<LocalDate>() {
			
			public String toString(LocalDate t) {
				if (t != null) {
					return formatter.format(t);
				}
				return null;
			}
			
			public LocalDate fromString(String string) {
				if (string != null && !string.trim().isEmpty()) {
					return LocalDate.parse(string, formatter);
				}
				return null;
			}

		});
	
	sGeral.setSub_Data(formatter.format(dpDataSubterranea.getValue()));
	
	System.out.println("valor da data " + sGeral.getSub_Data());
	
	}
	
	*/
	

}
